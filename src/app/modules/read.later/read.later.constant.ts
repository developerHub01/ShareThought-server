const READ_LATER_COLLECTION_NAME = "Read_Later";
const READ_LATER_SEARCHABLE_FIELD: Array<string> = [];

export const ReadLaterConstant = {
  READ_LATER_COLLECTION_NAME,
  READ_LATER_SEARCHABLE_FIELD,
};
